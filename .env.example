# AI生成（自然言語→ASCIIアート）の接続先 Worker の URL。
# 未設定の場合、AI生成は無効化され、手入力のASCIIアートのみ利用できます。
#
# デプロイ済み Worker を使う場合:
#   VITE_API_URL=https://ascii-art-reveal-api.<あなた>.workers.dev
#
# ローカルで `wrangler dev` を使う場合:
#   VITE_API_URL=http://localhost:8787
VITE_API_URL=
